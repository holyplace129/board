<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.learn.board.domain.gallery.infrastructure.GalleryMapper">

    <!-- 갤러리 생성 -->
    <insert id="save" parameterType="Gallery" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO gallery (name, display_name, description)
        VALUES (#{name}, #{displayName}, #{description})
    </insert>

    <!-- 갤러리 수정 -->
    <update id="update" parameterType="Gallery">
        UPDATE gallery
        SET
            display_name = #{displayName},
            description = #{description},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 갤러리 삭제 -->
    <delete id="deleteById">
        DELETE FROM gallery WHERE id = #{id}
    </delete>

    <!-- 갤러리 단건 조회 (ID 기준) -->
    <select id="findById" resultType="Gallery">
        SELECT id, name, display_name, description, created_at, updated_at
        FROM gallery
        WHERE id = #{id}
    </select>

    <!-- 갤러리 단건 조회 (영문 이름 기준) -->
    <select id="findByName" resultType="Gallery">
        SELECT id, name, display_name, description, created_at, updated_at
        FROM gallery
        WHERE name = #{name}
    </select>

    <!-- 갤러리 목록 조회 -->
    <select id="findAll" resultType="Gallery">
        SELECT id, name, display_name, description, created_at, updated_at
        FROM gallery
        ORDER BY id DESC
    </select>

</mapper>