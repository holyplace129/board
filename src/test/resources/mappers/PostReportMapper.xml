<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.learn.board.domain.report.infrastructure.ReportMapper">

    <!-- 게시글 신고 기록 저장 -->
    <insert id="save" parameterType="PostReport">
        INSERT INTO post_report (post_id, reporter_ip, reason_code, reason_detail)
        VALUES (#{postId}, #{reporterIp}, #{reasonCode}, #{reasonDetail})
    </insert>

    <!-- 특정 사용자의 특정 게시글 신고 여부 확인 (중복 방지용) -->
    <select id="findByPostIdAndReporterIp" resultType="PostReport">
        SELECT id, post_id, reporter_ip, reason_code, reason_detail, created_at
        FROM post_report
        WHERE post_id = #{postId} AND reporter_ip = #{reporterIp}
    </select>

</mapper>