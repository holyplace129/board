<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.learn.board.domain.post.infrastructure.PostVoteMapper">

    <!-- 게시글 투표 기록 저장 -->
    <insert id="save" parameterType="PostVote">
        INSERT INTO post_vote (post_id, voter_ip, vote_type)
        VALUES (#{postId}, #{voterIp}, #{voteType})
    </insert>

    <!-- 특정 사용자의 특정 게시글 투표 여부 확인 (중복 방지용) -->
    <select id="findByPostIdAndVoterIp" resultType="PostVote">
        SELECT id, post_id, voter_ip, vote_type, created_at
        FROM post_vote
        WHERE post_id = #{postId} AND voter_ip = #{voterIp}
    </select>

</mapper>