<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.learn.board.domain.post.infrastructure.PostImageMapper">

    <!-- 이미지 정보 저장 -->
    <insert id="save" parameterType="PostImage">
        INSERT INTO post_image (post_id, file_url, original_file_name, file_size_kb, sort_order)
        VALUES (#{postId}, #{fileUrl}, #{originalFileName}, #{fileSizeKb}, #{sortOrder})
    </insert>

    <!-- 특정 게시글에 첨부된 모든 이미지 정보 조회 -->
    <select id="findByPostId" resultType="PostImage">
        SELECT id, post_id, file_url, original_file_name, file_size_kb, sort_order, created_at
        FROM post_image
        WHERE post_id = #{postId}
        ORDER BY sort_order ASC
    </select>

    <!-- 게시글 삭제 시 관련 이미지 정보 일괄 삭제 -->
    <delete id="deleteByPostId">
        DELETE FROM post_image WHERE post_id = #{postId}
    </delete>

</mapper>