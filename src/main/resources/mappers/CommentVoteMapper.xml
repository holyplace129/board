<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.learn.board.domain.comment.infrastructure.CommentVoteMapper">

    <!-- 댓글 투표 기록 저장 -->
    <insert id="save" parameterType="CommentVote">
        INSERT INTO comment_vote (comment_id, voter_ip)
        VALUES (#{commentId}, #{voterIp})
    </insert>

    <!-- 특정 사용자의 특정 댓글 투표 여부 확인 (중복 방지용) -->
    <select id="findByCommentIdAndVoterIp" resultType="CommentVote">
        SELECT id, comment_id, voter_ip, created_at
        FROM comment_vote
        WHERE comment_id = #{commentId} AND voter_ip = #{voterIp}
    </select>

</mapper>